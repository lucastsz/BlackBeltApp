@model IEnumerable<BlackBeltApp.Models.SemiCampeonato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_aluno1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_aluno2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_aluno3)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_aluno4)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_aluno1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_aluno2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_aluno3)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_aluno4)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
