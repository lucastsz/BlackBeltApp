@model IEnumerable<BlackBeltApp.Models.Campeonato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_quarta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_semi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_final)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_vencedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_quarta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_semi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_final)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_vencedor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
